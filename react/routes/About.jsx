/** @jsx React.DOM */

var React = require('react'),
    D = React.DOM;

var About = React.createClass({
    render: function () {
        return (
            <h1>About!</h1>
        );
    }
});

module.exports = About;